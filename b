git clone 원격저장소주소 
-->파일생성후 터미널에서 사용하면 내가 github에 올린 코드를 복사 가능

git hub에서 comment를 단 코드는 내가 받아쓸때는 오류가 난다.
-->이를 해결하기 위해서 우리는 git push 전에 git pull을 써야한다.
git pull 원격저장소 주소
터미널에 위쪽 pull을 쓰고 git push를 하면 comment코드를 볼수 있습니다.
git pull branch 이
git fetch : 원격저장소 신규 commit 가져오세요